using System;
using vkSmartWall;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
namespace vkSmartWall.test
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            //var vkApi = new VkAPI();
            //var vkClient = new VkClient
            //var uid1 = "71985644";
            ////var uid2 = vkApi.GetUserById("mr.pavlichenkov").GetUid().ToString();

            //AppUser testUser = vkClient.GetUserById(uid1);
            //testUser.SetFriends(vkApi.GetFriends(testUser.GetUid().ToString()));


            //var mock = new Mock<VkAPI>();
            //mock.Setup(x => x.GetUserById(1.ToString())).Returns(
            //    new AppUser(1)); //uid
            //Assert.AreEqual(117, testUser.GetFriends().Count);
        }
    }
}
